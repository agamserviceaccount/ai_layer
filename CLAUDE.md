This project is an `ai_layer` tool written in TypeScript.

**Before you begin:**
* **Read @AGENTS.md** for your role and persona.
* **Consult @ai/project-context.md, @ai/standards.md, and @ai/skills/typescript.md** for essential project background, coding standards, and language-specific guidance.

**First files to review:**
1. `src/cli/index.ts` (CLI entry point)
2. `src/core/config.ts` (Core configuration)

**Commands:**
* Run npm run build to compile the project.
* Always run npm test before committing.

**Guardrails:**
* **Never** modify `package-lock.json`.
* **Always** avoid modifying generated files (e.g., in `dist/`).
* **DO NOT** modify `CLAUDE.md` (this file).

**Planning:**
* For any change affecting multiple files, always propose a plan first.